# MSGNet因子挖掘 项目进度

最后更新：2026-05-07

---

## 当前阶段：全流程已跑通 — 确认结构性结论

七个阶段全部完成。多头因子有效（测试集Sharpe 1.623），空头在静态/动态两种框架下均亏损，属于ETF多空结构的系统性Beta暴露问题，非可优化缺陷。

---

## 各阶段状态

| 阶段 | 内容 | 状态 |
|------|------|------|
| 一：数据准备 | ETF20数据+预处理，EFFECTIVE_START自动检测 | ✅ 完成 |
| 二：模型训练 | MSGNet ETF20，d_model=64，kernel=(45,1) | ✅ 完成 |
| 三：因子提取 | 三类因子CSV（静态截面快照） | ✅ 完成 |
| 四：因子验证 | IC检验 + Beta控制单因子收益显著性检验 | ✅ 完成 |
| 五：因子中性化+回测 | amplitude Beta中性化，多头有效验证 | ✅ 完成 |
| 六：策略回测 | Walk-Forward（静态因子，20窗口）| ✅ 完成 |
| 七：滚动更新 | Walk-Forward（逐Fold重训练，12个Fold）| ✅ 完成 |

---

## 阶段四：因子验证结果（etf20_1，未中性化）

### 通过门槛的因子（|IC均值|>0.03 且 |IC_IR|>0.5）

| 因子 | 最佳预测期 | IC均值 | IC_IR | 正向比例 | 方向 | 备注 |
|------|-----------|--------|-------|---------|------|------|
| amplitude | h=20 | -0.280 | -1.405 | 8.8% | 反向 | 含Beta暴露，已中性化 |
| in_degree_L2_S3 | h=10 | -0.213 | -1.065 | 15.7% | 反向 | 疑含Beta暴露 |
| pagerank_L2_S3 | h=10 | -0.198 | -0.970 | 18.3% | 反向 | 疑含Beta暴露 |
| period_2nd | h=20 | -0.188 | -0.899 | 18.5% | 反向 | — |
| period_stability | h=20 | +0.180 | +0.897 | 80.5% | 正向 | 真Alpha信号 |
| in_degree_L2_S2 | h=20 | -0.136 | -0.687 | 22.9% | 反向 | — |
| scale_diff_L1 | h=20 | +0.155 | +0.676 | 74.8% | 正向 | 真Alpha信号 |
| scale_diff_L1 | h=10 | +0.122 | +0.527 | 69.0% | 正向 | — |

---

## 阶段四补充：单因子收益显著性检验（Beta控制版）

已新增 `src/factor_return_test.py`，把截图中的“单因子显著性检验”改造为适合 ETF20 的版本：

1. 不做行业中性化：ETF本身就是行业/主题标的，行业中性化会剥离研究对象。
2. 只控制历史 Beta：用训练集收益计算每只ETF相对等权市场的Beta。
3. 因子先对Beta取残差，再做每日截面回归：`未来d日收益 = 常数项 + Beta暴露 + 因子残差暴露 + 残差`。
4. 对 d=1/5/10/20 重复检验，输出年化因子收益、IR、普通t值、Newey-West t值；显著门槛为 `|NW_t值| >= 2 且 |IR| >= 0.5`。

输出文件：`facts/runs/<run_id>/validation/factor_return_report.csv`

### 新增检验的主要结果

| 因子 | 最佳预测期 | 年化因子收益 | 因子IR | NW_t值 | 方向 |
|------|-----------|--------------|--------|--------|------|
| dominant_period | h=20 | -4.20% | -5.635 | -3.635 | 反向 |
| amplitude | h=20 | +3.31% | +4.536 | +2.838 | 正向 |
| pagerank_L1_S2 | h=20 | +4.04% | +4.404 | +2.799 | 正向 |
| in_degree_L1_S1 | h=20 | -3.69% | -4.255 | -2.652 | 反向 |
| in_degree_L1_S2 | h=20 | +4.01% | +3.965 | +2.552 | 正向 |
| scale_diff_L2 | h=20 | -3.45% | -3.865 | -2.381 | 反向 |

### 解释

- 新增检验确认：MSGNet图结构因子和周期因子在控制静态Beta后，仍能产生显著横截面因子收益。
- 该检验是对IC检验的增强，但不能直接替代组合回测；ETF20截面小，单因子回归方向对样本和口径敏感。
- `period_stability` 在原IC口径下表现较好，但在Beta控制的单因子收益口径下未通过显著性门槛（h=20：IR=1.137，NW_t=0.703），后续应优先作为组合辅助因子，而不是单独交易信号。

---

## 阶段五：回测结果

### 原始回测（未中性化，amplitude + period_stability IC加权）
- 全期：年化约 -30%，Sharpe 约 -1.5
- 问题：空头（高振幅ETF）在牛市中持续上涨，多空亏损

### Beta中性化后回测（amplitude截面回归取残差）

| 指标 | 全期 | 测试集（样本外） | 多头单独（测试集） |
|------|------|----------------|-----------------|
| 年化收益 | -14.63% | -17.05% | **+26.39%** |
| Sharpe | -1.512 | -2.102 | **1.623** |
| 最大回撤 | -58.43% | -20.15% | -9.47% |
| 日胜率 | 47.20% | 45.91% | 55.64% |

### 关键结论
- **多头 Sharpe=1.623，超过研究目标（>1.0）** ✅
- 低振幅+高稳定性 ETF 选股是真实有效的 Alpha 信号
- 空头仍亏损：静态 Beta 中性化精度有限，高振幅 ETF 含有未被剥离的暴露
- 中性化有效：全期年化从约-30%改善至-14.63%，方向正确但不彻底

### 待决策
- **方案A**：放弃空头，只做多头（Sharpe已达标，ETF无需融券）+ 沪深300对冲
- **方案B**：升级因子为时间序列 → 逐日动态Beta中性化 → 空头才能真正有效

---

## 阶段六：静态 Walk-Forward 结果（walk_forward.py）

- 窗口长度：63交易日（约3个月），共20个窗口
- 正收益窗口：2 / 20（10%）
- 结论：多头Low-Beta ETF集中，空头High-Beta ETF集中，纯粹Beta方向押注，牛市必亏

---

## 阶段七：滚动更新 Walk-Forward 结果（rolling_update.py）

| 指标 | 结果 |
|------|------|
| Fold数量 | 12 |
| 正收益Fold | 2 / 12（17%） |
| 平均年化收益 | -12.04% |
| 平均Sharpe | -1.189 |
| Sharpe标准差 | 1.223 |

### 关键结论
- 滚动重训练与静态因子结果一致，均约2/12正收益
- 根本原因：ETF20标的数N=20，TOP_PCT=20%只能选4只多头+4只空头；空头恒为高振幅高Beta ETF，牛市系统性上涨
- Beta中性化（N=20截面OLS，自由度极低）无法有效剥离暴露
- **结论：多空结构在ETF20上不可行，空头问题是结构性的，而非优化问题**
- **核心Alpha信号已验证：低振幅+高周期稳定性选股有效（多头测试集Sharpe=1.623）**

---

## 关键技术约束

| 约束 | 原因 |
|------|------|
| d_model ≥ N_ETF | GraphBlock Conv2d kernel=(d_model-c_out+1,1) 要求 |
| ETF20: d_model=64, batch_size=32 | kernel=(45,1)，感受野充裕；显存压力小 |
| num_workers=0 | Windows DataLoader多进程坑 |
| get_market_etf | 海外访问，其余接口封锁 |
| 静态因子中性化精度有限 | 截面一次性回归 vs 逐日动态回归 |

---

## 重新跑完整流程命令

在项目根目录执行：

```powershell
cd "C:\Users\INSATIABLE\Desktop\均线量化\MSGNet因子挖掘"
```

### 0. 环境准备

```powershell
python -m pip install -r requirements.txt
```

结果目录规则：

- `train.py` 会自动创建新目录：`facts/runs/<YYYYMMDD_HHMMSS>/`。
- 后续脚本自动写入最新运行目录，统一保存在同一个 `<run_id>` 下。
- 如果跳过训练但想新开一轮结果目录，先运行：

```powershell
python .\src\start_run.py
```

也可以用环境变量固定目录名：

```powershell
$env:MSGNET_RUN_ID="manual_001"
```

### 1. 标准主流程

```powershell
python .\src\data_pipeline.py
python .\src\preprocess.py
python .\src\train.py
python .\src\factor_extract.py
python .\src\factor_validate.py
python .\src\factor_return_test.py
python .\src\factor_neutralize.py
python .\src\backtest.py
python .\src\walk_forward.py
```

运行顺序说明：

1. `data_pipeline.py`：下载/增量更新 ETF20 行情，生成 `data/etf20/price.csv`。
2. `preprocess.py`：生成 MSGNet 输入 `returns.csv` 和 `returns.npy`。
3. `train.py`：训练 MSGNet，生成 checkpoint。
4. `factor_extract.py`：从 checkpoint 提取图结构因子、Embedding 因子、周期因子。
5. `factor_validate.py`：跑 IC 检验和分组分析。
6. `factor_return_test.py`：跑 Beta 控制单因子收益显著性检验。
7. `factor_neutralize.py`：对 `amplitude` 做 Beta 中性化。
8. `backtest.py`：跑静态多空/多头回测。
9. `walk_forward.py`：跑静态因子的 Walk-Forward 稳健性检验。

### 2. 耗时滚动重训练验证

如需复现阶段七，再单独运行：

```powershell
python .\src\rolling_update.py
```

该步骤会逐 Fold 重新训练 MSGNet，耗时明显长于标准主流程。输出目录为 `facts/runs/<run_id>/rolling_update/`。

### 3. 主要输出位置

| 步骤 | 输出 |
|------|------|
| 数据下载 | `data/etf20/price.csv` |
| 预处理 | `data/etf20/returns.csv`, `data/etf20/returns.npy` |
| 模型训练 | `facts/runs/<run_id>/checkpoints/` |
| 因子提取 | `facts/runs/<run_id>/factors/` |
| IC检验 | `facts/runs/<run_id>/validation/ic_report.csv` |
| 单因子收益显著性检验 | `facts/runs/<run_id>/validation/factor_return_report.csv` |
| 中性化因子 | `facts/runs/<run_id>/factors/neutralized/period_factors_neutralized.csv` |
| 静态回测 | `facts/runs/<run_id>/backtest/` |
| 静态Walk-Forward | `facts/runs/<run_id>/walk_forward/` |
| 滚动重训练Walk-Forward | `facts/runs/<run_id>/rolling_update/` |

最新运行目录记录在：`facts/runs/_latest_run.txt`。

---

## 已修复Bug记录

1. `np.Inf → np.inf`（NumPy 2.0删除）— `MSGNet-main/utils/tools.py:40`
2. out_degree排除 — softmax行归一化恒为1.0，无信息量
3. fillna downcast FutureWarning
4. START_DATE裁剪 — `build_price_table()`末尾
5. factor_extract.py root_path错误
6. adata成分股列名自动探测（排除index_code列）
7. data_pipeline.py target="date" 导致 ValueError
8. 分组收益用真实价格收益率而非z-score（修复负数复利）
9. matplotlib中文乱码（Microsoft YaHei字体）
10. EFFECTIVE_START自动检测 — 替代硬编码START_DATE，消除ffill引入的假零收益率
11. factor_extract.py SETTING串/d_model/subgraph_size未同步ETF20配置
12. 新增 `factor_return_test.py`：按截图方法改造单因子显著性检验，采用Beta控制、因子残差回归、Newey-West t值修正
